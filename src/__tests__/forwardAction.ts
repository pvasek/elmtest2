import * as test from 'tape';
import { Test } from 'tape';
import { forwardAction } from '../forwardAction';

test('forwardAction - should generate forward action', (t: Test) => {
    const type = 'ACTION1';
    const payload = {};
    //const result = forwardAction(;
    t.fail('NOT IMPLEMENTED');
    t.end(); 
});