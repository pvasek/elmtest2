import * as test from 'tape';
import { Test } from 'tape';
import { update } from '../Table';

test('table - should select item', (t: Test) => {
    // const type = 'ACTION1';
    // const payload = {};
    // const result = update(type, payload);
    // t.equal(type, result.type);
    // t.equal(payload, result.payload);
    t.end(); 
});